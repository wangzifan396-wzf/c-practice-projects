# CSDN 人工发布检查表

## 粘贴与配图

1. 打开 CSDN Markdown 编辑器并新建文章。
2. 将 `article-csdn.md` 全文粘贴到正文。
3. 搜索完整标记 `【配图 01：请上传 images/01-fig1.png 后删除本行】`，在该处上传 `images/01-fig1.png`，确认预览后删除整行标记。
   - 保留紧随其后的图注：双棋盘模型——`mine` 是标准答案，`show` 是玩家界面，二者用同一套坐标 `(行, 列)`，靠坐标一一对应。
4. 搜索完整标记 `【配图 02：请上传 images/02-fig2.png 后删除本行】`，在该处上传 `images/02-fig2.png`，确认预览后删除整行标记。
   - 保留紧随其后的图注：哨兵边框——11×11 数组里，外圈（高亮框外）全是占位的「没雷」，只有框内 9×9 是玩家真正能点的棋盘。
5. 搜索完整标记 `【配图 03：请上传 images/03-fig3.png 后删除本行】`，在该处上传 `images/03-fig3.png`，确认预览后删除整行标记。
   - 保留紧随其后的图注：3×3 邻域扫描——两层循环遍历中心周围的 9 格，跳过中心 `(x,y)` 自己，剩下的 8 个就是邻居。
6. 搜索完整标记 `【配图 04：请上传 images/04-fig4.png 后删除本行】`，在该处上传 `images/04-fig4.png`，确认预览后删除整行标记。
   - 保留紧随其后的图注：连锁展开——从一颗空格（中心）出发，蓝色越深离中心越近；遇到数字就停（出口 B），遇到空格继续向 8 邻居扩散（出口 C），已翻开则刹车（出口 A）。
7. 搜索完整标记 `【配图 05：请上传 images/05-fig5.png 后删除本行】`，在该处上传 `images/05-fig5.png`，确认预览后删除整行标记。
   - 保留紧随其后的图注：游戏主循环——拿到坐标后先判合法，再判是否已揭开，再判是否踩雷，最后数雷并决定展开或显示数字，直到胜利或踩雷。
8. 将 `images/cover.png` 设置为文章封面；正文中无需重复插入。

## 发布信息

- 标题：从零写一个能玩的 C 语言扫雷
- 文章类型：原创
- 推荐分区：C/C++
- 摘要：一份能编译、能玩、零告警的 C 语言扫雷是怎么写出来的？本文从一张白纸讲起，用双棋盘模型、哨兵边框、3×3 邻域扫描、递归连锁展开四个核心设计，带初学者一步步拼出完整程序。配 5 张自绘示意图（双棋盘、哨兵边框、数雷、递归水波、主循环流程图），代码已开源到 GitHub，本地一行 make 即可运行。适合正在写扫雷课设、或刚学完 C 语言想做个小游戏练手的同学。
- 标签：C语言、扫雷游戏、递归算法、C语言入门、小游戏开发
- 备选分区：编程语言、游戏开发
- 备选标题：
  - 手把手写出一个能玩的 C 语言扫雷：双棋盘、哨兵边框与递归展开
  - 从零做扫雷：一份可运行 C 语言小游戏的设计与实现
- 发布备注：
  - 封面 1280x720，正文配图 5 张均为 1280x720，自绘 SVG 经 sharp 栅格化。
  - 图1 双棋盘 / 图2 哨兵边框 / 图3 3x3 数雷 / 图4 递归展开 / 图5 主循环流程图，放置位置见文末「配图索引」。
  - 文中终端输出全部为真实运行记录，非手写示意。

## 最终核对

- [ ] 正文中已没有 `【配图】` 标记。
- [ ] 封面和每张正文图均清晰、顺序正确、没有隐私信息。
- [ ] 代码块、表格、链接和目录在预览中正常。
- [ ] 标题、摘要、分类、标签和原创类型已经填写。
- [ ] 已删除所有只给发布者看的备注。
- [ ] 已选择“保存草稿”或由作者本人确认公开发布。
